---

## 2. 问题 C：小规模但有说服力 Benchmark（强调可解释与可复现）
如何构建小规模但有说服力、可行性的 Benchmark
### 2.1 目标重定义

Benchmark 不以规模取胜，先回答：

**几何推理模块在“规则可解 + 指令多样 + 局部遮挡”场景下，是否稳定优于纯视觉策略。**

### 2.2 数据组成（小而精）

第一版建议双轨并行，总量 150~300：

- **轨道 1：规则可控集（主集合，约 70%）**  
  来源：CAD / 3D primitive / SVG。  
  作用：验证方法机制，确保 GT 干净。
- **轨道 2：现实扰动集（补充集合，约 30%）**  
  来源：真实场景抽象化样本或半自动校正样本。  
  作用：验证泛化，不让结论只停留在“理想图形”。

### 2.3 任务与分层（避免一次铺太大）

先固定一个主任务，两个可选扩展：

1. **主任务：桌面四角 Pointing**（必须）  
2. **扩展任务 A：遮挡补全 Pointing**（优先）  
3. **扩展任务 B：相对方位 + 距离词 Pointing**（如 front-left / mid distance）

按三维标签分层统计：

- 几何复杂度（规则/轻透视/复杂背景）  
- 指令复杂度（短指令/组合指令/歧义指令）  
- 遮挡强度（无/轻/中/重）

### 2.4 对比协议（可复现、可讲故事）

至少保留三条线：

- Baseline-1：纯视觉大模型 Pointing。  
- Baseline-2：视觉检测/分割 + 几何后处理（无显式推理链）。  
- Method：显式数学几何推理 + Pointing 决策。

并固定：

- 相同测试集与样本顺序。  
- 固定 Prompt 模板与温度等参数。  
- 每组至少重复 3 次，报告均值/方差，防止偶然性。

### 2.5 指标（服务“机制证明”）

- Point Error（L2）  
- Success@T（T=5/10/20 px 或归一化阈值）  
- Occlusion Split Success  
- Instruction Split Success（按指令类型分组）  
- Geometry Consistency（点序与几何约束残差）

### 2.6 C 阶段交付物（本周）

- 1 页结果主表（总指标 + 分层指标）。  
- 2~3 张关键可视化（遮挡分层、指令分层、典型错例）。  
- 1 份失败案例归因（数据噪声/指令歧义/几何假设失效）。

---

## 3. B 与 C 一体化策略（重点）

不是“先造完数据再评测”，而是小循环迭代：

1. 先做小批量数据（30~50）验证标注链路。  
2. 跑小评测找错误模式（特别是指令和遮挡场景）。  
3. 反向补齐对应数据，再扩到 150~300。  
4. 结论稳定后，才考虑重 pipeline 与更大规模。

---

## 4. 下周执行清单（按轻重缓急）

1. 完成“数据构建模式调研表”（重点看自动标注与 3D->2D 投影范式）。  
2. 建立候选数据池：CAD / 3D primitive / SVG 各至少 2~3 个来源。  
3. 统一最小数据格式与元数据字段（确保后续可分层评测）。  
4. 先产出 30~50 个样本，打通从数据到评测的最小闭环。  
5. 跑一次三方法对比，先看趋势，不急于追求最终 SOTA。  
6. 根据错例补样，扩展到 150~300，形成第一版 Benchmark。

---

## 5. 风险与应对（按当前反馈更新）

- 风险 1：公开数据与 Pointing 指令不匹配，难以直接使用。  
  - 应对：优先选择几何可控数据源，自建指令模板并做小规模校正。
- 风险 2：过早工程化导致方向锁死、后续调整成本高。  
  - 应对：先调研模式与小样本闭环，暂缓重 pipeline。
- 风险 3：数据“多而杂”，但结论依旧不清晰。  
  - 应对：坚持小规模可解释设计，围绕“几何推理是否有效”组织指标。

---

## 6. 当前阶段结论导向

从“做更多案例”转为“先摸清数据构建路径”。  
先把**可控数据 + 可复现评测 + 可解释结论**三件事做扎实，再决定是否扩大规模与工程化升级。